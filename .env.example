DATABASE_URL="postgres://postgres:postgres@localhost:5432/lummus"
BETTER_AUTH_SECRET="replace-with-32-bytes-base64"
BETTER_AUTH_URL="http://localhost:5174"
# Bearer token for POST /api/invites (and any future /api routes).
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))"
LUMMUS_API_TOKEN="replace-with-32-bytes-base64url"
